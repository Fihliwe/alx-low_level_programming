#include <stdio.h>
/**
 * main - a program that prints the alphabet in lowercase
 * Return - 0
 */
int main(void)
{
	char a;
	while
		(a <= 'z'){
			putchar(a);
			c++;

	putchar('\n');
	return (0);
}
